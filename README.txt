#Python Program to Fetch ten characters who appear most in the Star Wars films

First we need to install a few dependencies listed in the requirements file.From a Terminal/Command Prompt, execute:
1> run requirements.txt using 
pip install -r requirements.txt

2>Then run the main program(also set location indicate inside)